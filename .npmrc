# react-helmet-async (the only legacy dep) declares an old React peer range but is
# verified working under React 19. This lets fresh installs and remote/CI builds
# (e.g. Cloudflare Pages) resolve without manual --legacy-peer-deps flags.
legacy-peer-deps=true
